#include<stdio.h>
#include<conio.h>
#include<string.h>

void main()
{
    char s1[50]="Flowers";
    char s2[50]="are beautiful";
    char s3[100];
    int i;

    clrscr();

    printf("Length = %d\n",strlen(s1));

    strcpy(s3,s1);
    strcat(s3," ");
    strcat(s3,s2);
    printf("Concatenation = %s\n",s3);

    printf("Substring = ");
    for(i=1;i<=3;i++)
        printf("%c",s1[i]);

    printf("\nReplace = is beautiful");

    getch();
}