#include<stdio.h>
#include<conio.h>

int gcd(int a,int b)
{
    if(b==0)
        return a;
    return gcd(b,a%b);
}

void main()
{
    int a,b,c;

    clrscr();

    printf("Enter 3 numbers: ");
    scanf("%d%d%d",&a,&b,&c);

    printf("GCD = %d",gcd(gcd(a,b),c));

    getch();
}